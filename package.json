{
  "name": "reversecase",
  "version": "1.0.0",
  "description": "Takes string of alphanumeric characters and returns it in reverse order with inverted case (each character changes lower to upper and vice versa)",
  "main": "/src/index.ts",
  "scripts": {
    "build": "rm -rf lib/* && tsc",
    "run": "npm run build && node lib/index.js",
    "test": "npm run build && mocha"
  },
  "keywords": [],
  "author": "Samuel Sýkora",
  "license": "ISC",
  "devDependencies": {
    "@types/mocha": "^9.1.1",
    "@types/node": "^18.6.4",
    "assert": "^2.0.0",
    "mocha": "^10.0.0",
    "mochawesome": "^7.1.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "node": "^18.7.0"
  }
}
